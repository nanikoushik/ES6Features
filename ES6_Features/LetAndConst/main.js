// const should never change throughout the application
// 

//var type = "cupcake";

//the type is not going to change so instaed of var we can change that to const

const type = "cupcake";

let flavour = "Apple";
let price = 5;

type = "Cake" //not possible as type is const

flavour = "GreenApple" //we can change the value of flavour as it is let.